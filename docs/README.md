# docs/ — Raw reference for core tooling

The **RAW SOURCES** layer (Karpathy). Official documentation for *core* tooling we rely on across
projects (e.g. `uv/`, `spec-kit/`, `claude-code/`), organized by source and citing their URLs.
We mirror the source; if it changes, we re-fetch and log it — we never hand-edit to "fix."

> Project-specific API docs (e.g. a video provider used by one project) live in **that project's** repo,
> not here.

_Empty for now._
