# skills/

Reusable Claude Code **skill packs** (`SKILL.md` + supporting files) we collect from GitHub/elsewhere
or create ourselves. Each skill lives in its own subfolder. Skills here can be linked into
`~/.claude/skills/` so they're live in Claude Code.

_Empty for now — see `../index.md` for what's planned._
